const UserRoles = {
    ADMIN: "admin",
    SELLER: "seller",
    CUSTOMER: "customer"
}

const GlobalStatus = {
    ACTIVE: "active",
    INACTIVE: "inactive"
}

const Gender = {
    MALE: "male",
    FEMALE: "female",
    OTHER: "other"
}

module.exports = {
    UserRoles,
    GlobalStatus,
    Gender
}